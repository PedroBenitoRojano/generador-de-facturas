{
  "name": "invoiceflow-workspace",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "install-all": "concurrently \"npm install --prefix backend\" \"npm install --prefix frontend\" \"npm install --prefix cli\"",
    "fullstack": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "test": "npm test --prefix backend && npm test --prefix cli"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}